on: [push]

jobs:
  build_job:
    runs-on: ubuntu-latest
    name: A job to runn the build
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Build step
        uses: ./
